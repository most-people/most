import type {
  MobileCoreSnapshot,
  MostBoxMobileCore,
} from '../../mobileCore/types'

export type NodeConnectionPanelProps = {
  autoOpen?: boolean
  client: MostBoxMobileCore
  snapshot: MobileCoreSnapshot
}

export function NodeConnectionPanel(_props: NodeConnectionPanelProps) {
  return null
}
